import React from 'react';
import styled from 'styled-components';

const Header = () => {
  return (
    <Wrapper>
      <span>Project Holo</span>
    </Wrapper>
  );
};

const Wrapper = styled.div`
  padding: 21px;
`;

export default Header;
